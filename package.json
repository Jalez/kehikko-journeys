{
  "name": "kehikko-journeys",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The journeys themselves: what has to become true for a user, step by step, and where each piece of work stands. An app: a page, a store of its own and an MCP door, which a host may frame but nothing here needs one.",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "register": "bun run register.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "roadmap-module-protocol": "git+ssh://git@github.com/Jalez/kehikko-protocol.git#main",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0",
    "vite": "^7.2.4"
  }
}
